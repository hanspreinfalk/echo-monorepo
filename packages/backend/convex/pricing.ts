/** Monthly price of the Pro plan in USD (whole dollars). */
export const PRO_PRICE_USD = 99;

/** Monthly price of the Pro plan in cents (used for Stripe's unit_amount). */
export const PRO_PRICE_CENTS = PRO_PRICE_USD * 100;
